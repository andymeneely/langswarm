version: '3'

services:
  corenlp:
    image: nlpbox/corenlp
    ports:
      - "9000:9000"
    environment:
      - JAVA_XMX=1500M
    deploy:
      replicas: 6
      resources:
          limits:
            memory: 1800M
